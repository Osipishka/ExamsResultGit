<Window x:Class="ExamsResults.AddExamWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавление экзамена" Height="550" Width="800">

    <Border BorderBrush="#4CAF50" BorderThickness="3" Padding="30" Background="White">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Margin="20">

                <TextBlock Text="Предмет"/>
                <TextBox x:Name="SubjectBox"/>

                <TextBlock Text="Дата (дд.мм.гггг)" Margin="0,10,0,0"/>
                <TextBox x:Name="DateBox"/>

                <TextBlock Text="Экзаменатор" Margin="0,10,0,0"/>
                <TextBox x:Name="ExaminerBox"/>

                <TextBlock Text="Студент" Margin="0,10,0,0"/>
                <TextBox x:Name="StudentBox"/>

                <TextBlock Text="Группа" Margin="0,10,0,0"/>
                <TextBox x:Name="GroupBox"/>

                <TextBlock Text="Баллы" Margin="0,10,0,0"/>
                <TextBox x:Name="ScoreBox"/>

            </StackPanel>

            <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Отмена" Width="100" Height="40" Margin="0,0,10,0" 
                        Click="CancelButton_Click" Background="#f44336" Foreground="White">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="20,0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <Button Content="Добавить" Width="100" Height="40" 
                        Click="SaveButton_Click" Background="#4CAF50" Foreground="White">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="20,0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>